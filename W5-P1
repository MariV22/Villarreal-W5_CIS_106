# Initialize the starting number
num = 1

# Loop while the number is less than or equal to 25
while num <= 25:
    # Print the current odd number
    print(num)
    # Increment the number by 2 to get the next odd number
    num += 2
